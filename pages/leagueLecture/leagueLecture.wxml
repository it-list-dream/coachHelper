<view class="leagueContainer">
  <view class="league" wx:for="{{togetherList}}" wx:key="index">
     <image  data-index="{{index}}"  bindtap="previewBigImage" src="{{item.imgurl}}"></image>
  </view>
</view>
