<view class="r-wrap">
  <!-- 每一条数据 -->
  <view class="r-wrap-title">06.01</view>
  <view class="r1">
    <view class="r-wrap-item" wx:for="{{stepList}}">
      <!-- 左子项 -->
      <view class="r-item-left">
        <!-- 左子项上块 -->
        <view class="r-item-left-crd {{index== 0 ?'first':''}} "></view>
        <!-- 点 -->
        <view class="r-point-wrap">
          <view class="r-point-item"></view>
        </view>
        <!-- 左子项下块 -->
        <view class="r-item-left-crd {{index == stepList.length - 1?'last':''}}"></view>
      </view>
      <!-- 右子项 -->
      <view class="r-item-right">
        <!-- 内容 -->
        <view class="r-item-right-content">
          <view class="addRecordText" wx:if="{{index == 0}}" bindtap="addFollow">+添加跟进</view>
          <view class="r-item-text" wx:else>
            <view class="r-info">13:51</view>
            <view class="r-con">
              <view class="r-text">
                今天张三带来了很多人来训练,
                今天张三带来了很多人来训练,
                今天张三带来了很多人来训练,
                今天张三带来了很多人来训练,
                今天张三带来了很多人来训练,
                今天张三带来了很多人来训练
              </view>
              <view class="icon-box">
                <view bindtap="editText">
                  <image class="i26" src="/assets/images/edit1.png"></image>
                  <text class="icon-text">编辑</text>
                </view>
                <view class="last-icon" bindtap="deleteText">
                  <image class="i26" src="/assets/images/delete1.png"></image>
                  <text class="icon-text">删除</text>
                </view>
              </view>
            </view>
            <view class="r-info">王教练</view>
          </view>
        </view>
      </view>
    </view>
  </view>
  <ct-dialog is-show="{{isClose}}" bind:cancelEvent="cancel" bind:confirmEvent="confrim"></ct-dialog>
</view>