<view class="venueContainer">
  <view class="venue-item" wx:for="{{venueList}}" wx:key="index">
    <image data-index="{{index}}"  bindtap="previewBigImage" src="{{item.imgurl}}" mode="widthFix"></image>
  </view> 
</view>